# InsureBot Flask Backend

This is a simple Flask backend project.

## Setup

1. Run the bat file to install the required packages.
   ```powershell
   start.bat


## Project Structure
- `app.py` : Main Flask application
- `requirements.txt` : Python dependencies